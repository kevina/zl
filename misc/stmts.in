
(stmts
  (if (id if) "()" ... (id else) ...)
  (if (id if) "()" ...)
  (switch (id switch) "()" ...)
  (while (id while) "()" ...)
  (do (id do) ... (id while) "()")
  (for (id for) "()" ...)
  (break (id break))
  (continue (id continue))
  (return (id return) ...)
  (goto (id goto) ...)
  (noop))

...






...

"if" "(" {EXP} ")" {<stmt> {TOKEN}* . "else"?} ("else" {<stmt> {TOKEN} *})?

