
Syntax * test(Syntax * in, Environ * env) {
  Mark * fluid mark = new_mark;
  Syntax *a, *b;
  match(`[$a,$b], in);
  return `{1 + $a + $b;};
  //return `(plus $a $b);
}

make_macro test;


int main() {
  printf("%d\n", test(2,4));
  return 0;
}

